这是一个非常明智的调整方向。“商务科技风”（Business Tech / Enterprise SaaS）相比之前的“炫彩霓虹风”，更能传递**专业、可信、高效**的产品心智，特别适合 B 端用户或电商商家。

这种风格的标杆产品参考：**Linear, Vercel, Stripe Dashboard, Arc Browser, Datadog**。

以下为您撰写的**完整前端视觉重构 PRD（v5.0 商务科技版）**，以及对应的**核心代码实现方案**。

---

# 📘 SoraDirector 前端视觉重构 PRD (v5.0 - 商务科技版)

| 文档属性 | 内容 |
| --- | --- |
| **项目名称** | SoraDirector (AIvdeo) |
| **设计代号** | **"Nebula Pro" (星云专业版)** |
| **核心风格** | **商务科技 (Business Tech)**：克制、理性、高密度、秩序感 |
| **目标用户** | 电商运营、广告投放师、专业视频创作者 |

---

## 1. 设计愿景与原则 (Design Philosophy)

### 1.1 核心关键词

* **Rational (理性)**: 摒弃高饱和度的装饰性渐变，使用冷色调（深蓝、灰、白）为主轴，传达算法的精准。
* **Structure (秩序)**: 强调网格系统、分割线和层级关系，像驾驶舱仪表盘一样清晰。
* **Efficiency (高效)**: 减少非必要的留白和动效，提升信息密度，让用户专注于参数调整和脚本编写。

### 1.2 视觉降噪

* **去色**: 将原本大面积的紫色/粉色光晕移除，改为**点缀色**。
* **锐化**: 圆角从 `12px` (lg/xl) 调整为 `6px-8px` (sm/md)，显得更加干练、硬朗。
* **材质**: 使用微弱的“磨砂”和“内发光”代替大阴影，模拟精密仪器的质感。

---

## 2. 视觉规范系统 (Visual System)

### 2.1 色彩体系 (Color Palette)

我们采用 **"Slate & Sky" (岩板灰 & 天空蓝)** 配色方案。

* **Canvas (背景)**:
* Light Mode: `#F8FAFC` (Slate-50) - 极浅的灰蓝，避免纯白刺眼。
* Dark Mode: `#020617` (Slate-950) - 深邃的午夜蓝黑。


* **Surface (容器)**:
* Sidebar/Panels: `#FFFFFF` (Light) / `#0F172A` (Dark, Slate-900)。


* **Primary (主品牌色)**: `#0F172A` (Slate-900, 接近黑色的深蓝) —— 用于强调按钮、激活状态，代表权威。
* **Accent (科技点缀)**: `#0EA5E9` (Sky-500) 或 `#2563EB` (Blue-600) —— 用于高亮选区、AI 生成中的进度条、光标。
* **Border (边框)**: `#E2E8F0` (Slate-200) —— 细腻、高频使用，构建秩序感。

### 2.2 字体与排版 (Typography)

* **Font Family**: `Inter` (首选), `Roboto`, `PingFang SC`。
* **Weight**: 大量使用 `Medium (500)` 和 `Semibold (600)`，减少 `Bold (700)` 的使用，保持精致感。
* **Mono**: 脚本编辑器和参数面板使用 `JetBrains Mono` 或 `SF Mono`，强调“代码/指令”属性。

### 2.3 组件形态 (Component Shape)

* **Radius**: 全局统一 `rounded-md` (6px) 或 `rounded-lg` (8px)。严禁使用圆润的 `rounded-2xl`。
* **Shadows**: 使用 `shadow-sm` (极细微投影) 或 `shadow-none` (扁平化 + 边框)。

---

## 3. 界面布局重构 (Layout Strategy)

### 3.1 布局架构：经典三栏式 (The Workbench)

采用专业生产力工具的标准布局：

* **Left (Sidebar)**: `w-64`，固定，深色或高对比度。功能导航 + 资产库入口。
* **Center (Stage/Canvas)**: `flex-1`，浅色底。核心操作区（表单、脚本、画布）。
* **Right (Inspector/Preview)**: `w-96`，可折叠。实时预览、参数校验、AI 对话助手。

### 3.2 导航设计 (Navigation)

* **一级导航**: 侧边栏垂直排列，选中态使用左侧“高亮条” (`border-l-4`) 而非背景色块，更具商务感。
* **阶段指示器 (Stepper)**: 放置在 Center 顶部。使用 **“面包屑 + 进度条”** 的混合形态，明确当前所处环节。

---

## 4. 关键页面交互 (Key Interactions)

### 4.1 脚本创作 (Scripting)

* **双栏模式**: 左侧为结构化表单（场景、镜头、提示词），右侧为实时分镜预览。
* **AI 介入**: 当 AI 生成内容时，使用 **Typewriter (打字机)** 效果，配合蓝色光标闪烁，体现科技感。

### 4.2 视频生成 (Rendering)

* **HUD 界面**: 生成过程中，不再展示简单的 Loading 转圈，而是展示 **"System Layout"** —— 类似于终端的日志滚动 (`Initializing model...`, `Rendering frame 24/60...`)，增加用户的掌控感和等待耐心。

---

## 5. 代码实现方案 (Implementation)

以下是基于您现有文件结构修改的方案。

### 5.1 修改 `tailwind.config.js` (定义商务科技色板)

这里我们将颜色定义为 Slate (岩板灰) 色系，这是最符合“商务科技”的色系。

```javascript
/** @type {import('tailwindcss').Config} */
export default {
    darkMode: ["class"],
    content: ["./index.html", "./src/**/*.{ts,tsx,js,jsx}"],
    theme: {
    	extend: {
    		colors: {
    			border: 'hsl(var(--border))',
    			input: 'hsl(var(--input))',
    			ring: 'hsl(var(--ring))',
    			background: 'hsl(var(--background))',
    			foreground: 'hsl(var(--foreground))',
                
                // --- Business Tech Palette ---
                // 主品牌色 (深邃权威)
                primary: {
                    DEFAULT: 'hsl(var(--primary))',
                    foreground: 'hsl(var(--primary-foreground))',
                    900: '#0f172a', // Slate 900
                    800: '#1e293b',
                },
                // 科技点缀色 (电光蓝 - 活跃/AI)
                tech: {
                    DEFAULT: '#0ea5e9', // Sky 500
                    hover: '#0284c7',   // Sky 600
                    light: '#e0f2fe',   // Sky 100 (背景)
                },
                // 中性灰 (构建骨架)
                slate: {
                    50: '#f8fafc',  // 页面背景
                    100: '#f1f5f9', // 面板背景
                    200: '#e2e8f0', // 边框
                    500: '#64748b', // 次要文字
                    900: '#0f172a', // 主要文字
                },
                
                // 保留 shadcn 语义变量映射
    			secondary: {
    				DEFAULT: 'hsl(var(--secondary))',
    				foreground: 'hsl(var(--secondary-foreground))'
    			},
    			destructive: {
    				DEFAULT: 'hsl(var(--destructive))',
    				foreground: 'hsl(var(--destructive-foreground))'
    			},
    			muted: {
    				DEFAULT: 'hsl(var(--muted))',
    				foreground: 'hsl(var(--muted-foreground))'
    			},
    			accent: {
    				DEFAULT: 'hsl(var(--accent))',
    				foreground: 'hsl(var(--accent-foreground))'
    			},
    			popover: {
    				DEFAULT: 'hsl(var(--popover))',
    				foreground: 'hsl(var(--popover-foreground))'
    			},
    			card: {
    				DEFAULT: 'hsl(var(--card))',
    				foreground: 'hsl(var(--card-foreground))'
    			},
    		},
    		borderRadius: {
                // 商务风更锐利，减小圆角
    			lg: '0.5rem',    // 8px
    			md: '0.375rem',  // 6px
    			sm: '0.25rem'    // 4px
    		},
            fontFamily: {
                sans: ['Inter', 'system-ui', 'sans-serif'],
                mono: ['JetBrains Mono', 'SFMono-Regular', 'monospace'],
            },
            boxShadow: {
                // 精致的层级投影
                'tech-sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
                'tech-md': '0 4px 6px -1px rgba(15, 23, 42, 0.1), 0 2px 4px -1px rgba(15, 23, 42, 0.06)',
                'tech-glow': '0 0 15px rgba(14, 165, 233, 0.3)', // 蓝色科技光晕
            }
    	}
    },
    plugins: [require("tailwindcss-animate")],
}

```

### 5.2 修改 `src/styles/globals.css` (CSS 变量映射)

使用 Slate 色系替换原有的 Purple 色系，构建冷峻、理性的基调。

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    /* --- Business Tech Light Mode --- */
    
    /* 背景：极浅灰蓝，而非纯白，增加质感 */
    --background: 210 40% 98%;      /* Slate-50 */
    --foreground: 222 47% 11%;      /* Slate-900 */
 
    /* 卡片：纯白，通过边框区分 */
    --card: 0 0% 100%;
    --card-foreground: 222 47% 11%;
 
    --popover: 0 0% 100%;
    --popover-foreground: 222 47% 11%;
 
    /* 主色：深蓝 (权威、确定性) */
    --primary: 222 47% 11%;         /* Slate-900 */
    --primary-foreground: 210 40% 98%;
 
    /* 次级色：浅灰蓝 */
    --secondary: 210 40% 96.1%;     /* Slate-100 */
    --secondary-foreground: 222 47% 11%;
 
    /* 弱化色 */
    --muted: 210 40% 96.1%;
    --muted-foreground: 215 16% 47%; /* Slate-500 */
 
    /* 强调/科技色：天蓝 (AI、高亮、选中) */
    --accent: 199 89% 48%;          /* Sky-500 */
    --accent-foreground: 210 40% 98%;
 
    /* 破坏色 */
    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 210 40% 98%;
 
    /* 边框：清晰、克制 */
    --border: 214.3 31.8% 91.4%;    /* Slate-200 */
    --input: 214.3 31.8% 91.4%;
    --ring: 222 47% 11%;            /* Focus ring color */
 
    --radius: 0.5rem;
  }
 
  .dark {
    /* --- Business Tech Dark Mode (Deep Navy) --- */
    --background: 222 47% 11%;      /* Slate-950 */
    --foreground: 210 40% 98%;
 
    --card: 222 47% 11%;
    --card-foreground: 210 40% 98%;
 
    --popover: 222 47% 11%;
    --popover-foreground: 210 40% 98%;
 
    --primary: 210 40% 98%;
    --primary-foreground: 222 47.4% 11.2%;
 
    --secondary: 217.2 32.6% 17.5%; /* Slate-800 */
    --secondary-foreground: 210 40% 98%;
 
    --muted: 217.2 32.6% 17.5%;
    --muted-foreground: 215 20.2% 65.1%;
 
    --accent: 199 89% 48%;          /* Sky-500 */
    --accent-foreground: 210 40% 98%;
 
    --destructive: 0 62.8% 30.6%;
    --destructive-foreground: 210 40% 98%;
 
    --border: 217.2 32.6% 17.5%;    /* Slate-800 */
    --input: 217.2 32.6% 17.5%;
    --ring: 212.7 26.8% 83.9%;
  }
}

@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
    font-feature-settings: "rlig" 1, "calt" 1; /* 优化字体渲染 */
  }
}

/* 商务科技组件覆盖 */
@layer components {
  /* 科技感卡片：微描边，无阴影或极弱阴影 */
  .tech-card {
    @apply bg-card border border-border rounded-lg shadow-sm hover:shadow-md transition-all duration-200;
  }

  /* 科技感按钮：扁平，强调文字 */
  .btn-tech-primary {
    @apply bg-slate-900 text-white hover:bg-slate-800 active:scale-[0.98] shadow-sm;
  }
  
  .btn-tech-outline {
    @apply border border-slate-300 bg-transparent hover:bg-slate-50 text-slate-700;
  }

  /* 侧边栏导航项 */
  .nav-item {
    @apply flex items-center gap-3 px-3 py-2 rounded-md text-sm font-medium text-slate-600 transition-colors hover:text-slate-900 hover:bg-slate-100;
  }
  
  .nav-item.active {
    @apply bg-slate-100 text-slate-900 border-l-4 border-sky-500 rounded-none rounded-r-md pl-2; /* 左侧高亮条设计 */
  }
}

```

### 5.3 重构 `AppLayout.tsx` (实现新布局)

去除紫色渐变背景，使用更干净的白色/灰色背景，强调内容本身。

```tsx
import React from 'react';
import { cn } from '../../lib/utils';
import {
    LayoutDashboard, // 替换 Box，更商务
    Palette,
    Clapperboard,
    Aperture,      // 替换 Camera
    Users,
    FileCode,      // 替换 ScrollText
    Sparkles,
    ChevronRight,
    Command,       // 增加 CMD 图标
    CreditCard
} from 'lucide-react';
import { Button } from '../ui/button';

export type StepId = 'product' | 'style' | 'scene' | 'camera' | 'character' | 'script';

interface AppLayoutProps {
    children: React.ReactNode;
    previewPanel: React.ReactNode;
    activeStep: StepId;
    onStepChange: (step: StepId) => void;
}

const NAVIGATION_ITEMS: { id: StepId; label: string; icon: React.ElementType }[] = [
    { id: 'product', label: '产品建模', icon: LayoutDashboard },
    { id: 'style', label: '视觉定调', icon: Palette },
    { id: 'scene', label: '场景构建', icon: Clapperboard },
    { id: 'camera', label: '运镜控制', icon: Aperture },
    { id: 'character', label: '角色设定', icon: Users },
    { id: 'script', label: '智能脚本', icon: FileCode },
];

export function AppLayout({ children, previewPanel, activeStep, onStepChange }: AppLayoutProps) {
    return (
        <div className="flex h-screen w-full bg-[#F8FAFC] text-slate-900 font-sans overflow-hidden">
            
            {/* 1. Sidebar: 深色商务风格 (Dark Navy Sidebar) */}
            <aside className="w-64 flex flex-col bg-slate-900 text-slate-300 border-r border-slate-800 shrink-0">
                {/* Brand Header */}
                <div className="h-16 flex items-center px-6 border-b border-slate-800/50">
                    <div className="flex items-center gap-2.5">
                        <div className="size-8 rounded-md bg-sky-500 flex items-center justify-center text-white">
                            <Command className="size-5" />
                        </div>
                        <span className="text-lg font-semibold text-white tracking-tight">SoraDirector</span>
                    </div>
                </div>

                {/* Main Navigation */}
                <nav className="flex-1 py-6 px-3 space-y-1">
                    <div className="px-3 mb-2 flex items-center justify-between">
                        <span className="text-[11px] font-bold text-slate-500 uppercase tracking-wider">Workspace</span>
                    </div>
                    
                    {NAVIGATION_ITEMS.map((item) => {
                        const isActive = activeStep === item.id;
                        const Icon = item.icon;

                        return (
                            <button
                                key={item.id}
                                onClick={() => onStepChange(item.id)}
                                className={cn(
                                    "w-full flex items-center justify-between px-3 py-2.5 rounded-md text-[13px] font-medium transition-all duration-200 group",
                                    isActive
                                        ? "bg-sky-500/10 text-sky-400"
                                        : "text-slate-400 hover:text-slate-100 hover:bg-slate-800"
                                )}
                            >
                                <div className="flex items-center gap-3">
                                    <Icon className={cn("size-[18px]", isActive ? "text-sky-400" : "text-slate-500 group-hover:text-slate-300")} />
                                    <span>{item.label}</span>
                                </div>
                                {isActive && <ChevronRight className="size-4 opacity-50" />}
                            </button>
                        );
                    })}
                </nav>

                {/* Bottom User Area */}
                <div className="p-4 border-t border-slate-800 bg-slate-900/50">
                     <div className="flex items-center gap-3 mb-4 px-1">
                        <div className="flex flex-col">
                             <span className="text-xs text-slate-400">Credits Balance</span>
                             <span className="text-sm font-mono font-bold text-white">2,450</span>
                        </div>
                        <Button size="sm" variant="outline" className="ml-auto h-7 text-xs border-slate-600 text-slate-300 hover:bg-slate-800 hover:text-white">
                            充值
                        </Button>
                     </div>
                </div>
            </aside>

            {/* 2. Main Workspace: 干净的灰白底色 */}
            <main className="flex-1 flex flex-col min-w-0 bg-[#F8FAFC] relative">
                {/* Top Header / Breadcrumb */}
                <header className="h-16 border-b border-slate-200 bg-white/80 backdrop-blur-sm sticky top-0 z-30 px-8 flex items-center justify-between">
                    <div className="flex items-center gap-2 text-sm text-slate-500">
                        <span>Project / New Video</span>
                        <ChevronRight className="size-4" />
                        <span className="font-medium text-slate-900">
                            {NAVIGATION_ITEMS.find(i => i.id === activeStep)?.label}
                        </span>
                    </div>
                    
                    <div className="flex items-center gap-3">
                        <span className="flex items-center gap-1.5 text-xs font-medium text-slate-500 bg-slate-100 px-2 py-1 rounded">
                            <span className="size-2 rounded-full bg-emerald-500 animate-pulse"></span>
                            System Ready
                        </span>
                    </div>
                </header>

                {/* Content Area */}
                <div className="flex-1 overflow-y-auto custom-scrollbar p-8">
                    <div className="max-w-5xl mx-auto">
                        <div className="mb-8">
                            <h1 className="text-2xl font-semibold text-slate-900 tracking-tight">
                                {NAVIGATION_ITEMS.find(i => i.id === activeStep)?.label}
                            </h1>
                            <p className="text-slate-500 text-sm mt-1">
                                Configure the parameters below to control the AI generation output.
                            </p>
                        </div>
                        
                        {/* 容器增加白色卡片背景和微投影 */}
                        <div className="bg-white rounded-lg border border-slate-200 shadow-sm p-6 min-h-[500px]">
                            {children}
                        </div>
                    </div>
                </div>
            </main>

            {/* 3. Inspector Panel: 右侧预览区 */}
            <aside className="w-[400px] flex flex-col border-l border-slate-200 bg-white relative z-20 shadow-[-4px_0_15px_-3px_rgba(0,0,0,0.02)]">
                <div className="h-16 flex items-center px-6 border-b border-slate-100">
                    <span className="text-sm font-semibold text-slate-900">Preview & Logs</span>
                </div>
                <div className="flex-1 overflow-y-auto p-6 bg-slate-50/50">
                    {previewPanel}
                </div>
            </aside>
        </div>
    );
}

```

### 总结变更点

1. **侧边栏 (Sidebar)**: 从浅色变为 **深蓝 (Slate-900)**，符合 B 端工具（如 Linear, VS Code）的沉浸式导航习惯，与亮色内容区形成鲜明对比。
2. **主色调 (Primary)**: 从紫色 (`#6366f1`) 改为 **天蓝 (`#0ea5e9`)** 和 **岩板灰 (`#0f172a`)**，去除了娱乐感，增强了科技感。
3. **字体与圆角**: 字体颜色更深（高对比度），圆角更小（更严谨）。
4. **布局**: 增加了 Header 面包屑导航，强调层级路径；右侧面板背景微调，增加层次感。